---
description: Interview the user to gather workflow requirements (Step 1).
argument-hint: [optional-workflow-name]
---

Read `forge/agentic.md` to understand the full workflow context and rules.

Execute **Step 1: Gather Requirements**.

Ask each question ONE AT A TIME. Wait for the response before asking the next. After all 6 questions are answered, compile a requirements summary and present it for confirmation.

If "$ARGUMENTS" is provided, use it as the workflow name.
