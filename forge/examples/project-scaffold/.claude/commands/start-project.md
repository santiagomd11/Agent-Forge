---
description: Start the full project scaffolding workflow.
argument-hint: [optional-project-name]
---

Read `agentic.md` to understand the full workflow context and rules.

Start creating a new project scaffold. If provided, use "$ARGUMENTS" as the project name.

Follow the workflow from Step 1 through Step 4, pausing at each approval gate ⏸.

Do not skip any steps. Present output for approval at every gate before continuing.
