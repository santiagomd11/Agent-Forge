# {{WORKFLOW_NAME}}, Workflow Orchestrator

## Trigger Commands

- `{{TRIGGER_1}}`
- `{{TRIGGER_2}}`
<!-- Add natural-language triggers users might say, plus the slash command -->

---

## Workflow Overview

```
{{WORKFLOW_DIAGRAM}}
```
<!-- ASCII diagram showing step sequence. Mark approval gates with ⏸ below the step name.
     Keep it to 1-2 lines. Example:

     Step 1         Step 2         Step 3         Step 4
     Gather    -->  Design    -->  Generate  -->  Deliver
                    ⏸                             ⏸
-->

---

{{FOR_EACH_STEP}}

## Step {{STEP_NUMBER}}: {{STEP_NAME}}
<!-- One step per phase of work. Step names: Title Case, describe WHAT not HOW.
     If the step has an approval gate, add ⏸ to the heading. -->

**Step file:** `agent/steps/step_{{STEP_NUMBER_PADDED}}_{{STEP_COMMAND}}.md`

Read the step file and execute it. {{STEP_BRIEF_DESCRIPTION}}

<!-- The step file contains: prompt reference, inputs, workflow actions, and required outputs.
     At runtime, outputs go to output/{run_id}/agent_outputs/step_NN_agent_output.md (inter-step context)
     and output/{run_id}/user_outputs/step_NN/ (deliverables). The API creates these directories
     automatically before execution starts. -->

---

{{END_FOR_EACH_STEP}}

## After Each Step

**ALWAYS ask for user approval before saving/marking complete:**

1. Show output to user
2. Ask: "Does this look good? Any changes needed?"
3. **Wait for user approval**
   - If changes requested → Modify and show again
   - If approved → Continue
4. Save files
5. Confirm: "Saved"

**NEVER proceed to the next step without user confirmation.**

---

## Scripts

Scripts in `agent/scripts/src/` support this workflow. A Python venv is pre-configured
at `agent/scripts/.venv/` with all dependencies installed.

{{SCRIPTS_LIST}}
<!-- List each script with a one-line description. Example:
     - `gen_html.py` -- Generates HTML reports from analysis data.
     - `fetch_data.py` -- Fetches records from the project API.
     If no workflow-specific scripts, write: "Built-in export scripts only (gen_document.py, gen_xlsx.py)." -->

To run a script:
```bash
source agent/scripts/.venv/bin/activate
python agent/scripts/src/{script_name}.py
```

To run tests:
```bash
source agent/scripts/.venv/bin/activate
python -m pytest agent/scripts/tests/
```

---

## Output Structure

```
{{OUTPUT_TREE}}
```
<!-- Show the complete folder/file tree. Standard layout:
     agent/Prompts/ (NN_Agent_Name.md)
     agent/steps/ (step_NN_step-name.md)
     agent/scripts/ (src/, tests/, requirements.txt, README.md)
     agent/utils/ (code/, docs/)
     output/ (at runtime: output/{run_id}/agent_outputs/ and output/{run_id}/user_outputs/ created per run) -->

---

{{IF_COMPUTER_USE}}

## Computer Use Configuration

This workflow includes steps that execute autonomously via the Computer Use Engine.

**Engine Initialization:** The engine detects the platform at runtime (WSL2, Linux, Windows, macOS) and loads the correct backend automatically.

**Available Methods:**
- `engine.screenshot()` -- capture full screen
- `engine.click(x, y)` / `engine.double_click(x, y)` / `engine.right_click(x, y)`
- `engine.type_text(text)` -- type a string
- `engine.key_press(*keys)` -- press key combination (e.g., "ctrl", "s")
- `engine.scroll(x, y, amount)` -- scroll at position
- `engine.drag(start_x, start_y, end_x, end_y)` -- drag
- `engine.find_element(description)` -- locate UI element by name
- `engine.get_screen_size()` -- get display dimensions

**Execution Commands:**
- `/execute-workflow` -- run this workflow via computer use
- `/pause-execution` -- pause mid-execution
- `/resume-execution` -- resume paused execution

**Config:** `computer_use/config.yaml` -- platform, provider, and action settings.

{{END_IF_COMPUTER_USE}}
<!-- Remove this section if the workflow does not use computer use. -->

---

## Quality Checks

| Step | Check |
|------|-------|
{{QUALITY_CHECKS}}
<!-- One row per step. Each check is a quick verification question.
     Example: "01 | All requirements gathered? Summary approved?" -->
