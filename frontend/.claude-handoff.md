# Claude Session Handoff

You are continuing work on the Agent Forge React frontend. You are a senior UX/UI engineer and senior frontend developer. The user is asleep and expects everything to be perfect when they wake up.

## FIRST STEP
Use computer use to visually test EVERY page after the aesthetic changes that were made but not properly verified. Click through all flows like a real human. Compare each page against the mockup reference image.

## Mockup Reference
The UI mockup is at: /home/santiago/Santiago/Common/Agent-Forge/ChatGPT Image 6 mar 2026, 10_25_57 p.m..png
Read this file first to see what the UI should look like. It shows 4 panels: Dashboard, Task Editor, Project Canvas, Run Viewer.

## Running Servers
- Backend: port 8000 (already running, if not: `python3 -m uvicorn api.main:app --host 127.0.0.1 --port 8000` from /home/santiago/Santiago/Common/Agent-Forge)
- Frontend: port 3000 (already running, if not: `source /home/santiago/.nvm/nvm.sh && npm run dev` from frontend/)
- Node.js via NVM: always run `source /home/santiago/.nvm/nvm.sh` before npm/npx commands
- sudo password: 31241113

## Branch
`feature/frontend` - committed locally, NOT pushed (user has no git credentials, they push manually)
DO NOT merge to master - user said they will do it.

## Tech Stack
- React 19 + TypeScript + Vite 7 + Tailwind CSS v4
- React Flow (@xyflow/react v12) for DAG canvas
- TanStack Query v5 for API state
- Vitest + Testing Library (38 unit tests), Playwright (11 E2E tests)
- Vite proxy: /api -> http://127.0.0.1:8000, /ws -> ws://127.0.0.1:8000

## What Was Done (verified working with computer use)
- Full React frontend built: Dashboard, Tasks, Projects, Canvas, Runs, Run Viewer, Settings, Task Editor
- Created tasks through UI: "Intent Classifier" (task type), "Escalation Review" (approval type)
- Added nodes to Customer Support Bot canvas (8 nodes total)
- Connected nodes with edges via drag on React Flow handles
- Clicked Run on canvas -> navigated to Run Viewer with pipeline visualization
- Node Configuration side panel works on click
- All tests pass (38 unit + 11 E2E + production build)

## What Was NOT Verified After Changes
Aesthetic changes were made to the color scheme but interactive flows were NOT re-tested afterward:
- Changed theme from purple accent to green accent (#22c55e)
- Deepened all background colors (bg-primary: #0b0e14, bg-secondary: #141821)
- Added card shadows, sidebar glow, hover effects on dashboard cards
- Updated Sidebar active state styling
These need to be visually verified with computer use on every page.

## Known Issues
1. Many "E2E Pipeline" and "E2E Test Task" entries cluttering UI (from Playwright tests)
2. Canvas node dragging sometimes moves node instead of creating edge (need precise handle targeting)
3. The Customer Support Bot project doesn't show up on first page of dashboard because E2E test projects push it down

## What Still Needs Doing
1. VERIFY all pages look correct after aesthetic changes - use computer use on each page
2. Compare each page against the mockup reference and fix any remaining visual gaps
3. Test creating a NEW project from scratch through the UI
4. Test the full flow: create project -> add tasks to canvas -> connect edges -> run -> view run
5. If anything looks off compared to the mockup, fix it
6. Run tests after any changes: `npx vitest run` and `npx playwright test` and `npm run build`

## Key Files
- Theme: frontend/src/index.css (@theme directive with CSS custom properties)
- Components: frontend/src/components/ui/ (Button, Card, Badge, Input, Select, TextArea, Toggle)
- Sidebar: frontend/src/components/Sidebar.tsx
- Pages: frontend/src/pages/ (Dashboard, Tasks, TaskEditor, TaskDetail, Projects, ProjectCanvas, Runs, RunViewer, Settings)
- API client: frontend/src/api/ (client.ts, tasks.ts, projects.ts, runs.ts)
- Types: frontend/src/types/index.ts
- Tests: frontend/src/**/__tests__/*.test.ts(x) and frontend/e2e/*.spec.ts

## Survival Strategy
If you run low on context, open a new terminal tab using computer use, start `claude --dangerously-skip-permissions` with a prompt to read this file and continue. Instruct it to kill the old terminal as its first step.

## User's Original Request
"you will be a senior UX/UI engineer and senior frontend developer, TDD, playwright tests, test the app by creating project/task by using computer use capabilities, good practices, SOLID, integrate with the back, test e2e using computer use, make it perfect, aesthetic matters - use the image reference"
